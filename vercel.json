{
  "installCommand": "cd frontend && npm ci --include=dev && cd ../backend && npm ci --include=dev",
  "buildCommand": "cd frontend && npm run build && cd ../backend && npm run vercel-build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/backend/api/index.ts"
    }
  ]
}
